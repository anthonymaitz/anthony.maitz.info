# Markdown-Driven Portfolio Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convert the hardcoded portfolio section in `index.html` to a markdown-driven system where each project lives in its own `.md` file, add URL discipline filtering, and fix the `/pdf` redirect in Vite dev/preview.

**Architecture:** A Node.js build script reads `projects/*.md` YAML-frontmatter files and regenerates the portfolio article content in `index.html` between comment markers. The home and resume sections stay as static HTML. Client-side JS reads `?discipline=` from the URL and auto-filters the portfolio on load. A Vite plugin inline in `vite.config.js` redirects `/pdf` → `/resume.pdf` in both dev and preview servers.

**Tech Stack:** Node.js ESM, `gray-matter` (YAML frontmatter parser, new devDependency), Vite 5, vanilla JS / jQuery 3.3.1 (existing CDN)

---

## File Structure

**Create:**
- `projects/` — 20 `.md` files, one per portfolio project
- `scripts/build-portfolio.js` — reads `projects/`, generates portfolio HTML, splices into `index.html` between markers
- `scripts/test-build-portfolio.js` — Node assert tests for `generatePortfolioHTML`

**Modify:**
- `package.json` — add `gray-matter` devDependency, add `build-portfolio` script and `prebuild` hook
- `index.html` — add `<!-- PORTFOLIO_GENERATED_START/END -->` markers around portfolio items; add URL param JS to `init()`
- `vite.config.js` — add inline Vite plugin with `configureServer` + `configurePreviewServer` to redirect `/pdf`

---

## Context

`index.html` is the compiled single-page app. The `<article id="portfolio">` section contains:
1. A `<div id="portfolio-filters">` with filter buttons — **keep this, it is NOT generated by the build script**
2. Individual `<div class="portfolio-item" data-discipline="...">` entries — **these ARE replaced by the build script**

The build script finds two HTML comment markers inside `<article id="portfolio">`:
```
<!-- PORTFOLIO_GENERATED_START -->
...items...
<!-- PORTFOLIO_GENERATED_END -->
```
Everything between the markers is replaced with freshly generated HTML on each run.

**Media path convention:** Images are referenced as `../media/filename.jpg` in `index.html`. Going "up" from root normalizes to `/media/filename.jpg` in browsers, which is where the `media/` directory is served. Preserve this convention.

**Fancybox caption system:** On page load, `$('a.hidden').each(function(){$(this).data('caption', $(this).text());})` reads the text content of each hidden gallery `<a>` element as its caption. Media items that have a `caption` field in frontmatter need their text placed as the text node inside the `<a>` tag.

**`data-discipline` attribute:** Uses the attribute name `data-discipline` (singular) on `.portfolio-item` divs. The `filterPortfolio` JS reads it via jQuery's `.data('discipline')`. The value is space-separated discipline slugs (e.g., `"interactive product"`).

---

### Task 1: Install gray-matter + create all project .md files

**Files:**
- Modify: `package.json`
- Create: `projects/jetpack-geography.md`
- Create: `projects/forevr.md`
- Create: `projects/insummary.md`
- Create: `projects/ucsf.md`
- Create: `projects/playsets.md`
- Create: `projects/shine.md`
- Create: `projects/qcon.md`
- Create: `projects/qbo.md`
- Create: `projects/nmfg.md`
- Create: `projects/collagio.md`
- Create: `projects/youareatourist.md`
- Create: `projects/adidasallin.md`
- Create: `projects/bornfree.md`
- Create: `projects/startraders.md`
- Create: `projects/igotalot.md`
- Create: `projects/ayyo.md`
- Create: `projects/thenaturebetweenus.md`
- Create: `projects/theexecutionofsolomonharris.md`
- Create: `projects/whentwoislandsmeet.md`
- Create: `projects/hutton.md`

**Frontmatter schema:**
- `slug` — unique identifier, matches the `data-fancybox` value and media file prefix
- `title` — human-readable display name (used as img alt and tooltip)
- `disciplines` — YAML array of discipline slugs: `interactive`, `product`, `game-production`, `production`
- `order` — integer, controls display order in the portfolio grid
- `stub` — boolean, `true` for projects with no images yet (shows a placeholder card)
- `featured` — boolean, currently informational only (home section is static HTML)
- `featured_order` — integer 1–3, currently informational only
- `no_trigger` — boolean, `true` for Hutton which has gallery items but no visible grid card
- `media` — YAML sequence; strings are filenames (relative to `media/`) or external URLs; objects have `src` and optional `caption`

- [ ] **Step 1: Install gray-matter**

```bash
cd /Users/anthonymaitz/Repositories/anthony.maitz.info && npm install --save-dev gray-matter
```

Expected: `package.json` devDependencies now includes `gray-matter`.

- [ ] **Step 2: Create projects/ directory and all 20 project files**

Create `projects/jetpack-geography.md`:
```markdown
---
slug: jetpack-geography
title: Jetpack Geography!
disciplines: [interactive, game-production]
order: 1
stub: true
featured: true
featured_order: 1
---
```

Create `projects/forevr.md`:
```markdown
---
slug: forevr
title: ForeVR Games
disciplines: [game-production]
order: 2
stub: true
---
```

Create `projects/insummary.md`:
```markdown
---
slug: insummary
title: Insummary
disciplines: [product]
order: 3
stub: true
---
```

Create `projects/ucsf.md`:
```markdown
---
slug: ucsf
title: UCSF Virtual Mentor
disciplines: [interactive]
order: 4
media:
  - ucsf-0.jpg
  - src: https://youtu.be/DA7rIcd8XNI
    caption: "We tested with medical workers in Kenya."
  - src: ucsf-1.jpg
    caption: "This is a poster made to explain how the virtual mentor works for the SL@B judges."
---
```

Create `projects/playsets.md`:
```markdown
---
slug: playsets
title: Playsets
disciplines: [interactive, product]
order: 5
featured: true
featured_order: 3
media:
  - playsets-0.jpg
  - "https://youtu.be/mNgjgNSpVGA&list=PL5AjSG-LQwc6m96V_t2jgG_08UElFIhmi"
  - playsets-1.jpg
  - playsets-2.jpg
  - playsets-3.jpg
  - playsets-4.jpg
  - playsets-5.jpg
  - playsets-6.gif
  - playsets-7.jpg
  - playsets-8.jpg
  - playsets-9.gif
  - playsets-10.gif
  - playsets-11.gif
  - playsets-12.jpg
  - playsets-13.jpg
---
```

Create `projects/shine.md`:
```markdown
---
slug: shine
title: Shine
disciplines: [interactive]
order: 6
featured: true
featured_order: 2
media:
  - shine-0.jpg
  - shine-1.jpg
---
```

Create `projects/qcon.md`:
```markdown
---
slug: qcon
title: QCon Conference Wearables
disciplines: [interactive]
order: 7
media:
  - qcon-0.jpg
  - https://youtu.be/DEkq6Hr71OA
  - https://youtu.be/Mpgg2dFjg5A
  - https://youtu.be/xZFSkbJ-aJ0
  - https://youtu.be/W0xyrc0oGZo
  - qcon-1.jpg
  - qcon-2.jpg
  - qcon-3.jpg
  - qcon-4.jpg
  - qcon-5.jpg
---
```

Create `projects/qbo.md`:
```markdown
---
slug: qbo
title: Q-bo Connected Coffee Machine
disciplines: [interactive]
order: 8
media:
  - qbo-0.jpg
  - https://youtu.be/hVk3VHgqY0w
  - qbo-1.jpg
  - qbo-2.jpg
  - qbo-3.jpg
  - qbo-4.jpg
  - qbo-5.jpg
  - qbo-6.m4v
---
```

Create `projects/nmfg.md`:
```markdown
---
slug: nmfg
title: Natural Markets Food Group
disciplines: [product]
order: 9
media:
  - nmfg-0.jpg
  - https://youtu.be/yG28QGNVWkA
  - nmfg-1.jpg
  - nmfg-2.jpg
  - nmfg-3.jpg
  - nmfg-4.jpg
  - nmfg-5.jpg
  - nmfg-6.jpg
  - nmfg-7.jpg
---
```

Create `projects/collagio.md`:
```markdown
---
slug: collagio
title: Collagio
disciplines: [interactive, product]
order: 10
media:
  - collagio-0.jpg
  - collagio-2.jpg
  - collagio-3.jpg
  - collagio-4.jpg
  - collagio-5.jpg
  - collagio-6.jpg
  - collagio-7.jpg
---
```

Create `projects/youareatourist.md`:
```markdown
---
slug: youareatourist
title: "You Are a Tourist"
disciplines: [production]
order: 11
media:
  - youareatourist-0.jpg
  - https://youtu.be/qkk5wViJo-I
  - youareatourist-1.jpg
  - youareatourist-2.jpg
  - youareatourist-3.jpg
  - youareatourist-4.jpg
  - youareatourist-5.jpg
  - youareatourist-6.jpg
  - youareatourist-7.jpg
  - youareatourist-8.jpg
  - youareatourist-9.jpg
  - youareatourist-10.jpg
  - youareatourist-11.jpg
  - youareatourist-12.jpg
---
```

Create `projects/adidasallin.md`:
```markdown
---
slug: adidasallin
title: Adidas All In
disciplines: [production]
order: 12
media:
  - adidasallin-0.jpg
  - https://youtu.be/DCRihtIZZdM
  - adidasallin-1.jpg
  - adidasallin-2.jpg
  - adidasallin-3.jpg
  - adidasallin-4.jpg
  - adidasallin-5.jpg
  - adidasallin-6.jpg
  - adidasallin-7.jpg
  - adidasallin-8.jpg
  - adidasallin-9.jpg
  - adidasallin-10.jpg
---
```

Create `projects/bornfree.md`:
```markdown
---
slug: bornfree
title: Born Free
disciplines: [production]
order: 13
media:
  - bornfree-0.jpg
  - https://vimeo.com/11219730
  - bornfree-1.jpg
  - bornfree-2.jpg
  - bornfree-3.jpg
  - bornfree-4.jpg
  - bornfree-5.jpg
  - bornfree-6.jpg
---
```

Create `projects/startraders.md`:
```markdown
---
slug: startraders
title: Star Traders
disciplines: [interactive]
order: 14
media:
  - startraders-0.jpg
  - startraders-1.jpg
  - startraders-3.jpg
  - startraders-4.jpg
  - startraders-5.jpg
  - startraders-6.jpg
  - startraders-7.jpg
  - startraders-8.jpg
---
```

(Note: `startraders-2.jpg` is intentionally omitted — it was skipped in the original HTML.)

Create `projects/igotalot.md`:
```markdown
---
slug: igotalot
title: I Got a Lot
disciplines: [production]
order: 15
media:
  - igotalot-0.jpg
  - https://youtu.be/z9bzjhnGrCQ
  - igotalot-2.jpg
  - igotalot-3.jpg
  - igotalot-4.jpg
  - igotalot-5.jpg
  - igotalot-6.jpg
---
```

(Note: `igotalot-1.jpg` is intentionally omitted — it was skipped in the original HTML.)

Create `projects/ayyo.md`:
```markdown
---
slug: ayyo
title: Ayyo
disciplines: [production]
order: 16
media:
  - ayyo-0.jpg
  - https://youtu.be/td6CnGR36Eo
  - ayyo-1.jpg
  - ayyo-2.jpg
  - ayyo-3.jpg
  - ayyo-4.jpg
  - ayyo-5.jpg
  - ayyo-6.jpg
  - ayyo-7.jpg
  - ayyo-8.jpg
  - ayyo-9.jpg
---
```

Create `projects/thenaturebetweenus.md`:
```markdown
---
slug: thenaturebetweenus
title: The Nature Between Us
disciplines: [production]
order: 17
media:
  - thenaturebetweenus-0.jpg
  - https://vimeo.com/3371232
  - thenaturebetweenus-1.jpg
  - thenaturebetweenus-2.jpg
  - thenaturebetweenus-3.jpg
  - thenaturebetweenus-4.jpg
  - thenaturebetweenus-5.jpg
  - thenaturebetweenus-6.jpg
  - thenaturebetweenus-7.jpg
  - thenaturebetweenus-8.jpg
---
```

Create `projects/theexecutionofsolomonharris.md`:
```markdown
---
slug: theexecutionofsolomonharris
title: The Execution of Solomon Harris
disciplines: [production]
order: 18
media:
  - theexecutionofsolomonharris-0.jpg
  - http://vimeo.com/20337358
  - theexecutionofsolomonharris-1.jpg
  - theexecutionofsolomonharris-2.jpg
  - theexecutionofsolomonharris-3.jpg
  - theexecutionofsolomonharris-4.jpg
---
```

Create `projects/whentwoislandsmeet.md`:
```markdown
---
slug: whentwoislandsmeet
title: When Two Islands Meet
disciplines: [production]
order: 19
media:
  - whentwoislandsmeet-0.jpg
  - whentwoislandsmeet-1.jpg
  - whentwoislandsmeet-2.jpg
  - whentwoislandsmeet-3.jpg
  - whentwoislandsmeet-4.jpg
  - whentwoislandsmeet-5.jpg
  - whentwoislandsmeet-6.jpg
---
```

Create `projects/hutton.md`:
```markdown
---
slug: hutton
title: Hutton 2.0
disciplines: [interactive]
order: 20
no_trigger: true
media:
  - hutton-1.png
  - hutton-2.jpg
  - hutton-3.jpg
---
```

(Note: `no_trigger: true` means this project has no visible grid card — only hidden gallery items. It matches the original HTML behavior where Hutton has no trigger image in the portfolio grid.)

- [ ] **Step 3: Verify all 20 files exist**

```bash
ls /Users/anthonymaitz/Repositories/anthony.maitz.info/projects/ | wc -l
```

Expected: `20`

- [ ] **Step 4: Commit**

```bash
cd /Users/anthonymaitz/Repositories/anthony.maitz.info
git add package.json package-lock.json projects/
git commit -m "feat: add gray-matter and project .md files for all portfolio entries"
```

---

### Task 2: Build script with tests

**Files:**
- Create: `scripts/build-portfolio.js`
- Create: `scripts/test-build-portfolio.js`

The build script exports `generatePortfolioHTML(projectsDir)` as a named export so it can be tested independently. When run directly via `node scripts/build-portfolio.js`, it also splices the output into `index.html`.

- [ ] **Step 1: Write the test file first**

Create `scripts/test-build-portfolio.js`:

```js
import assert from 'assert'
import path from 'path'
import { fileURLToPath } from 'url'
import { generatePortfolioHTML } from './build-portfolio.js'

const __dirname = path.dirname(fileURLToPath(import.meta.url))
const projectsDir = path.resolve(__dirname, '../projects')

const html = generatePortfolioHTML(projectsDir)

// All slugs should appear somewhere in the output
const slugs = [
  'jetpack-geography', 'forevr', 'insummary', 'ucsf', 'playsets', 'shine',
  'qcon', 'qbo', 'nmfg', 'collagio', 'youareatourist', 'adidasallin',
  'bornfree', 'startraders', 'igotalot', 'ayyo', 'thenaturebetweenus',
  'theexecutionofsolomonharris', 'whentwoislandsmeet', 'hutton',
]
for (const slug of slugs) {
  assert.ok(html.includes(`"${slug}"`), `Missing slug in output: ${slug}`)
}

// Stubs: have stub card, no trigger image
assert.ok(html.includes('>Jetpack Geography!</div>'), 'jetpack stub card text')
assert.ok(html.includes('>ForeVR Games</div>'), 'forevr stub card text')
assert.ok(html.includes('>Insummary</div>'), 'insummary stub card text')
assert.ok(!html.includes('data-trigger="jetpack-geography"'), 'jetpack must not have trigger')
assert.ok(!html.includes('data-trigger="forevr"'), 'forevr must not have trigger')

// Projects with media: have trigger img and gallery items
assert.ok(html.includes('data-trigger="playsets"'), 'playsets has trigger')
assert.ok(html.includes('data-trigger="shine"'), 'shine has trigger')
assert.ok(html.includes('data-fancybox="playsets"'), 'playsets has gallery items')
assert.ok(html.includes('data-fancybox="shine"'), 'shine has gallery items')

// Disciplines on portfolio-item wrappers
assert.ok(html.includes('data-discipline="interactive game-production"'), 'jetpack disciplines')
assert.ok(html.includes('data-discipline="interactive product"'), 'playsets disciplines')
assert.ok(html.includes('data-discipline="production"'), 'production discipline present')

// Captions: ucsf media items have text inside the <a>
assert.ok(html.includes('We tested with medical workers in Kenya.'), 'ucsf caption 1')
assert.ok(html.includes('This is a poster made to explain'), 'ucsf caption 2')

// Hutton: no visible trigger, but has hidden gallery items
assert.ok(!html.includes('data-trigger="hutton"'), 'hutton must not have trigger')
assert.ok(html.includes('data-fancybox="hutton"'), 'hutton has gallery items')

// Order: jetpack (order 1) before forevr (order 2) before insummary (order 3)
const jIdx = html.indexOf('"jetpack-geography"')
const fIdx = html.indexOf('"forevr"')
const iIdx = html.indexOf('"insummary"')
assert.ok(jIdx < fIdx && fIdx < iIdx, 'projects ordered by order field')

// URL-safe: & in playsets URL must be HTML-encoded as &amp;
assert.ok(html.includes('&amp;list='), 'playsets YouTube URL has & encoded as &amp;')

// Local media paths use ../media/ prefix
assert.ok(html.includes('../media/playsets-0.jpg'), 'local media path convention')
assert.ok(html.includes('../media/qbo-6.m4v'), 'local m4v media path')

console.log('All tests passed ✓')
```

- [ ] **Step 2: Run the test to confirm it fails (build-portfolio.js doesn't exist yet)**

```bash
cd /Users/anthonymaitz/Repositories/anthony.maitz.info && node scripts/test-build-portfolio.js
```

Expected: Error — `Cannot find module './build-portfolio.js'` or similar.

- [ ] **Step 3: Write build-portfolio.js**

Create `scripts/build-portfolio.js`:

```js
import fs from 'fs'
import path from 'path'
import { fileURLToPath } from 'url'
import matter from 'gray-matter'

const __dirname = path.dirname(fileURLToPath(import.meta.url))
const projectsDir = path.resolve(__dirname, '../projects')
const indexPath = path.resolve(__dirname, '../index.html')

const PORTFOLIO_START = '<!-- PORTFOLIO_GENERATED_START -->'
const PORTFOLIO_END = '<!-- PORTFOLIO_GENERATED_END -->'

function htmlEncode(str) {
  return String(str).replace(/&/g, '&amp;').replace(/"/g, '&quot;')
}

function isExternalUrl(src) {
  return src.startsWith('http://') || src.startsWith('https://')
}

function mediaHref(src) {
  return isExternalUrl(src) ? src : `../media/${src}`
}

function renderProject(project) {
  const { slug, title, disciplines, stub, no_trigger, media = [] } = project
  const disciplineAttr = disciplines.join(' ')

  if (stub) {
    return `<div class="portfolio-item" data-discipline="${disciplineAttr}">
<div class="portfolio-stub-card">${title}</div>
</div>\n`
  }

  const allItems = media.map(item =>
    typeof item === 'string' ? { src: item, caption: '' } : { src: item.src || '', caption: item.caption || '' }
  )

  let trigger = ''
  let galleryItems = allItems

  if (!no_trigger && allItems.length > 0) {
    const first = allItems[0]
    const href = htmlEncode(mediaHref(first.src))
    const alt = htmlEncode(title)
    trigger = `<a href="#${slug}" data-trigger="${slug}"><img src="${href}" title="${alt}" alt="${alt}"></a>\n`
    galleryItems = allItems.slice(1)
  }

  const galleryHTML = galleryItems.map(item => {
    const href = htmlEncode(mediaHref(item.src))
    const inner = item.caption ? `\n${item.caption}\n` : ''
    return `<a href="${href}" class="hidden" data-fancybox="${slug}" alt="">${inner}</a>\n`
  }).join('')

  return `<div class="portfolio-item" data-discipline="${disciplineAttr}">\n${trigger}${galleryHTML}</div>\n`
}

export function generatePortfolioHTML(dir) {
  const files = fs.readdirSync(dir)
    .filter(f => f.endsWith('.md'))
    .map(f => {
      const raw = fs.readFileSync(path.join(dir, f), 'utf8')
      return matter(raw).data
    })
    .sort((a, b) => (a.order ?? 999) - (b.order ?? 999))

  return files.map(renderProject).join('\n')
}

if (process.argv[1] === fileURLToPath(import.meta.url)) {
  const portfolioHTML = generatePortfolioHTML(projectsDir)

  let indexHTML = fs.readFileSync(indexPath, 'utf8')
  const startIdx = indexHTML.indexOf(PORTFOLIO_START)
  const endIdx = indexHTML.indexOf(PORTFOLIO_END)

  if (startIdx === -1 || endIdx === -1) {
    console.error(`Markers not found in index.html. Add these comments:\n  ${PORTFOLIO_START}\n  ${PORTFOLIO_END}`)
    process.exit(1)
  }

  const before = indexHTML.slice(0, startIdx + PORTFOLIO_START.length)
  const after = indexHTML.slice(endIdx)
  fs.writeFileSync(indexPath, `${before}\n${portfolioHTML}${after}`)

  const count = (portfolioHTML.match(/class="portfolio-item"/g) || []).length
  console.log(`Portfolio section updated: ${count} projects`)
}
```

- [ ] **Step 4: Run the test to verify it passes**

```bash
cd /Users/anthonymaitz/Repositories/anthony.maitz.info && node scripts/test-build-portfolio.js
```

Expected output:
```
All tests passed ✓
```

- [ ] **Step 5: Commit**

```bash
cd /Users/anthonymaitz/Repositories/anthony.maitz.info
git add scripts/build-portfolio.js scripts/test-build-portfolio.js
git commit -m "feat: build script to generate portfolio section from project .md files"
```

---

### Task 3: Wire build script into index.html and package.json

**Files:**
- Modify: `index.html` — add portfolio section markers, remove old loose `corollafaceoff` `<a>` tags
- Modify: `package.json` — add `build-portfolio` script and `prebuild` hook

The portfolio filters `<div id="portfolio-filters">` stay in `index.html` exactly as-is — the build script does NOT touch them. Only the `.portfolio-item` divs between the new markers are replaced.

Current portfolio article structure (simplified):
```html
<article id="portfolio">
<div id="portfolio-filters">...</div>
<div class="portfolio-item" ...>...</div>   ← these get replaced
...more items...
<!-- loose corollafaceoff <a> tags (dead code) -->
</article>
```

After this task:
```html
<article id="portfolio">
<div id="portfolio-filters">...</div>
<!-- PORTFOLIO_GENERATED_START -->
...generated items...
<!-- PORTFOLIO_GENERATED_END -->
</article>
```

- [ ] **Step 1: Add `build-portfolio` script and `prebuild` to package.json**

Open `package.json`. The current `scripts` section is:
```json
"scripts": {
  "dev": "vite",
  "build": "vite build && cp -r media dist/ && cp favicon.ico dist/ && node scripts/generate-pdf.js",
  "preview": "vite preview"
},
```

Replace with:
```json
"scripts": {
  "dev": "vite",
  "build-portfolio": "node scripts/build-portfolio.js",
  "prebuild": "node scripts/build-portfolio.js",
  "build": "vite build && cp -r media dist/ && cp favicon.ico dist/ && node scripts/generate-pdf.js",
  "preview": "vite preview"
},
```

- [ ] **Step 2: Replace the portfolio items section in index.html**

In `index.html`, find the line:
```html
<article id="portfolio">
```

The content of that article currently starts with `<div id="portfolio-filters">` then has all the `.portfolio-item` divs, then some loose `<a>` tags for `corollafaceoff` (dead code), and ends with `</article>`.

Replace everything from the line after the closing `</div>` of `<div id="portfolio-filters">...</div>` to just before `</article>` with the markers. The result should be:

```html
<article id="portfolio">
<div id="portfolio-filters">
  <button class="filter-btn selected" data-filter="all" onclick="filterPortfolio('all')">All</button>
  <button class="filter-btn" data-filter="interactive" onclick="filterPortfolio('interactive')">Interactive &amp; Game Design</button>
  <button class="filter-btn" data-filter="product" onclick="filterPortfolio('product')">Product</button>
  <button class="filter-btn" data-filter="game-production" onclick="filterPortfolio('game-production')">Game Production</button>
  <button class="filter-btn" data-filter="production" onclick="filterPortfolio('production')">Production Design</button>
</div>
<!-- PORTFOLIO_GENERATED_START -->
<!-- PORTFOLIO_GENERATED_END -->

</article>
```

The entire block of `.portfolio-item` divs and the loose `corollafaceoff` `<a>` tags is removed and replaced by the two comment markers. The `portfolio-filters` div is preserved exactly.

- [ ] **Step 3: Run the build script to populate the markers**

```bash
cd /Users/anthonymaitz/Repositories/anthony.maitz.info && npm run build-portfolio
```

Expected output:
```
Portfolio section updated: 20 projects
```

- [ ] **Step 4: Verify index.html has the correct portfolio items**

```bash
grep -c 'class="portfolio-item"' /Users/anthonymaitz/Repositories/anthony.maitz.info/index.html
```

Expected: `20`

```bash
grep 'data-discipline' /Users/anthonymaitz/Repositories/anthony.maitz.info/index.html | head -5
```

Expected: lines like `<div class="portfolio-item" data-discipline="interactive game-production">` for jetpack first.

- [ ] **Step 5: Start dev server and verify portfolio displays correctly in browser**

```bash
cd /Users/anthonymaitz/Repositories/anthony.maitz.info && npm run dev &
sleep 2
```

Open `http://localhost:5173` (or 5174 if 5173 is in use). Click "portfolio" in the nav. Verify:
- All portfolio projects are visible in the grid
- Filter buttons work (click "Product" → only product items show)
- Clicking a project image opens the Fancybox lightbox with gallery
- Stub cards (Jetpack, ForeVR, Insummary) show placeholder text

- [ ] **Step 6: Commit**

```bash
cd /Users/anthonymaitz/Repositories/anthony.maitz.info
git add index.html package.json
git commit -m "feat: wire build-portfolio into index.html with generated markers"
```

---

### Task 4: URL discipline param on page load

**Files:**
- Modify: `index.html` — update `init()` function to read `?discipline=` from URL

When a user visits `anthony.maitz.info/?discipline=product`, the page should automatically navigate to the portfolio tab and apply the product filter. This enables shareable URLs tailored to specific audiences.

Valid discipline values (same as filter button data-filter values):
- `interactive`
- `product`
- `game-production`
- `production`
- `all`

If the param is absent or empty, behavior is unchanged (show home tab).

- [ ] **Step 1: Update the `init` function in index.html**

Find this in `index.html`:
```js
function init(){
    $('nav').show();
    navctrl('home');
    $('a.hidden').each(function(){$(this).data('caption', $(this).text());});
}
```

Replace with:
```js
function init(){
    $('nav').show();
    $('a.hidden').each(function(){$(this).data('caption', $(this).text());});
    var params = new URLSearchParams(window.location.search);
    var discipline = params.get('discipline');
    if (discipline) {
        navctrl('portfolio');
        filterPortfolio(discipline);
    } else {
        navctrl('home');
    }
}
```

- [ ] **Step 2: Verify in browser with URL param**

With the dev server still running, navigate to `http://localhost:5173/?discipline=product`

Expected: Portfolio tab is shown immediately, only product items are visible, the "Product" filter button is highlighted.

Navigate to `http://localhost:5173/?discipline=production`

Expected: Portfolio tab shown, only production design items visible, "Production Design" button highlighted.

Navigate to `http://localhost:5173/` (no param)

Expected: Home tab shown as usual.

- [ ] **Step 3: Commit**

```bash
cd /Users/anthonymaitz/Repositories/anthony.maitz.info
git add index.html
git commit -m "feat: auto-filter portfolio on load via ?discipline= URL param"
```

---

### Task 5: Fix /pdf route redirect in Vite dev and preview

**Files:**
- Modify: `vite.config.js` — add inline Vite plugin with `configureServer` and `configurePreviewServer`

Currently, visiting `/pdf` in dev or preview serves `index.html` (Vite SPA fallback) instead of redirecting to `/resume.pdf`. The `public/pdf/index.html` meta-refresh trick works on GitHub Pages but not in Vite's dev/preview servers.

The fix: an inline Vite plugin that registers `/pdf` middleware on both the dev server and the preview server, redirecting to `/resume.pdf`.

Note: In dev mode, `/resume.pdf` doesn't exist (PDF is only generated by `npm run build`). The redirect will 404 for the PDF file in dev — that's acceptable. The redirect itself will work correctly.

- [ ] **Step 1: Update vite.config.js**

Replace the entire content of `vite.config.js` with:

```js
import { defineConfig } from 'vite'

const pdfRedirect = {
  name: 'pdf-redirect',
  configureServer(server) {
    server.middlewares.use('/pdf', (_req, res) => {
      res.writeHead(302, { Location: '/resume.pdf' })
      res.end()
    })
  },
  configurePreviewServer(server) {
    server.middlewares.use('/pdf', (_req, res) => {
      res.writeHead(302, { Location: '/resume.pdf' })
      res.end()
    })
  },
}

export default defineConfig({
  base: '/',
  build: {
    outDir: 'dist',
  },
  plugins: [pdfRedirect],
})
```

- [ ] **Step 2: Verify the redirect works in dev**

With dev server running:
```bash
curl -v http://localhost:5173/pdf 2>&1 | grep -E 'Location:|HTTP/'
```

Expected output includes:
```
< HTTP/1.1 302 Found
< Location: /resume.pdf
```

- [ ] **Step 3: Verify the redirect works in preview**

```bash
cd /Users/anthonymaitz/Repositories/anthony.maitz.info && npm run build 2>&1 | tail -5
```

Then:
```bash
npm run preview &
sleep 2
curl -v http://localhost:4173/pdf 2>&1 | grep -E 'Location:|HTTP/'
```

Expected:
```
< HTTP/1.1 302 Found
< Location: /resume.pdf
```

Also verify that navigating to `http://localhost:4173/pdf` in a browser downloads `resume.pdf` (or shows 404 briefly before redirecting — acceptable).

Kill the preview server after testing: `pkill -f "vite preview"` or `kill %` if using job control.

- [ ] **Step 4: Commit**

```bash
cd /Users/anthonymaitz/Repositories/anthony.maitz.info
git add vite.config.js
git commit -m "fix: redirect /pdf to /resume.pdf in Vite dev and preview servers"
```

---

## Self-Review

### Spec coverage check

User requests from session:
1. ✅ Individual `.md` files per portfolio project — Task 1 creates 20 project files
2. ✅ Resume assemblable from projects (foundation) — `disciplines` and `order` metadata enables future assembly; actual resume HTML left static as scoped
3. ✅ URL variables to tailor portfolio to industries — Task 4 implements `?discipline=` param
4. ✅ `/pdf` doesn't work — Task 5 fixes the redirect

### Placeholder scan

No TBDs, no "implement later", no steps without code. The test file (Task 2 Step 1) specifies exact assertions with expected values.

### Type consistency check

- `slug` field used consistently: `project.slug` everywhere in `renderProject`
- `disciplines` is always an array in frontmatter → `disciplines.join(' ')` produces `data-discipline` value
- `media` items: always normalized to `{ src, caption }` objects in `renderProject` before use
- Markers: `PORTFOLIO_START` / `PORTFOLIO_END` constants in build script match exactly what Task 3 puts in `index.html`
- `filterPortfolio(discipline)` called with a string in Task 4 — matches existing function signature which expects a string
