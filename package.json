{
  "name": "anthony-maitz-info",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build-portfolio": "node scripts/build-portfolio.js",
    "build-resume": "node scripts/build-resume.js",
    "build-contact": "node scripts/build-contact.js",
    "build-manifesto": "node scripts/build-manifesto.js",
    "prebuild": "node scripts/build-portfolio.js && node scripts/build-resume.js && node scripts/build-contact.js && node scripts/build-manifesto.js",
    "build": "vite build && cp -r media dist/ && cp favicon.ico dist/ && node scripts/generate-pdf.js || true",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@imgly/background-removal-node": "^1.4.5",
    "gray-matter": "^4.0.3",
    "puppeteer": "^24.42.0",
    "sharp": "^0.34.5",
    "vite": "^5.4.0"
  }
}
